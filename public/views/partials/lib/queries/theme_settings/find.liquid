{% liquid
  if name == blank
    return null
  endif

  graphql r  = 'modules/theme-manager/theme_settings/search', name: name

  assign results = r.records.results.first.settings
  if results
    hash_assign results['theme'] = r.records.results.first.name
  endif
  return results
%}
